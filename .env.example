# Base URL used by the server-side/typed API client (src/lib/api-client.ts)
NEXT_PUBLIC_API_BASE_URL=http://localhost:3000/api/v1

# Example of a server-only secret (never prefixed with NEXT_PUBLIC_)
API_SECRET_KEY=change-me
